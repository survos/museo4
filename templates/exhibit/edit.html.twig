{% extends 'base.html.twig' %}

{% block title %}Edit Exhibit{% endblock %}

{% block body %}
<main role="main">

    <div class="container">


    <h1>Edit Exhibit {{ exhibit.id }}</h1>

    {{ include('exhibit/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('exhibit_index') }}">back to list</a>

    {{ include('exhibit/_delete_form.html.twig') }}
</div>
</main>
    {% endblock %}
